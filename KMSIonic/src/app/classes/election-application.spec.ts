import { ElectionApplication } from './election-application';

describe('ElectionApplication', () => {
  it('should create an instance', () => {
    expect(new ElectionApplication()).toBeTruthy();
  });
});
