import { ClaimProfileRequest } from './claim-profile-request';

describe('ClaimProfileRequest', () => {
  it('should create an instance', () => {
    expect(new ClaimProfileRequest()).toBeTruthy();
  });
});
