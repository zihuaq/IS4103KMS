import { MaterialResourceAvailable } from './material-resource-available';

describe('MaterialResourceAvailable', () => {
  it('should create an instance', () => {
    expect(new MaterialResourceAvailable()).toBeTruthy();
  });
});
