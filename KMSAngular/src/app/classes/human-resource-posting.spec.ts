import { HumanResourcePosting } from './human-resource-posting';

describe('HumanResourcePosting', () => {
  it('should create an instance', () => {
    expect(new HumanResourcePosting()).toBeTruthy();
  });
});
