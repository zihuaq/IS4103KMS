import { MaterialResourcePosting } from './material-resource-posting';

describe('MaterialResourcePosting', () => {
  it('should create an instance', () => {
    expect(new MaterialResourcePosting()).toBeTruthy();
  });
});
